[build]
  command = "npm run build"
  publish = "dist"

# The service worker file itself must never be cached by the browser/CDN,
# or updates to it (and therefore to the app) can be delayed indefinitely.
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
